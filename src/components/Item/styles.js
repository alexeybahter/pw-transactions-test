import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    maxHeight: 220,
  },
  title: {
    fontSize: 14,
  },
  pos: {
    marginBottom: 12,
  },
});